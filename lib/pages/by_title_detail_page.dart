import 'package:flutter/material.dart';

class ByTitleDetailPage extends StatelessWidget {
  const ByTitleDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("By Title Detail"),
      ),
    );
  }
}