import 'package:flutter/material.dart';

class ByAuthorDetailPage extends StatelessWidget {
  const ByAuthorDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("By Author Detail"),
      ),
    );
  }
}